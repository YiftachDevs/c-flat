mod rng {
    fun next() -> usize {
        static mut seed: usize = 91237;
        seed = seed * 6364136223846793005 + 1442695040888963407;
        (seed >> 33)
    }
}

impl<T: Numeral> Range<T> {
    fun rand(&self) -> T {
        rng:next() as T % (self.end - self.start) + self.start
    }
}