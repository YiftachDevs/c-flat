import std/net.cf;
import std/error.cf;
import std/io.cf;
import std/time.cf;

fun main() -> i32 {
    let opt_listener_1 = TCP_Listener:listen(8080, 1);
    let opt_listener = TCP_Listener:listen(8080, 2);
    // if opt_listener.is_err() {
    //     println(&opt_listener.unwrap_err().message);
    //     return -1;
    // }
    // let listener = opt_listener.unwrap();
    *mem:heap_count() as i32
}