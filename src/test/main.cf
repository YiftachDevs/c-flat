fun main() {
    let nothing = Nothing { };
    let vec = Vec_2<f32> { 0.3, 0.4 };
    let vec_wrapper = Wrapper<Vec_2<f32>> { vec };
    debug<f32>(vec_wrapper.value.y)
}

struct Wrapper<T: i32> { value: T }
struct Vec_2<T> { x: T, y: T }
struct Nothing { }

fun debug<T>(value: T) {

}