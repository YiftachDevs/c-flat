import std/core.cf;
import std/io.cf;

fun main() -> u64 {
    {
        let mut vec = Vec<Vec<i32>>:new();
        vec.push(Vec<i32>:from(&[1, 2, 3]));
        vec.push(Vec<i32>:from(&[1, 2, 3]));
        vec.push(Vec<i32>:from(&[1, 2, 3]));
    };
    *mem:heap_count()
}