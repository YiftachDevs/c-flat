fun main() {
    let vec = math.Vec_2<f32> { 5.0, 6.0 };
    let x = math.get_x<f32>(vec);
}

mod math {
    struct Vec_2<T> { x: T, y: T }
    // i32 ? not
    // Vec_2<i32> no
    impl<T> Vec_2<Vec_2<T>> {
        fun get_x(vec: Vec_2<T>) -> T {
            vec.x
        }
    }
}