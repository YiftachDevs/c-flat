import std/io.cf;
import std/math.cf;
import std/rng.cf;
import std/time.cf;
import std/system.cf;
import game.cf;

fun main() -> usize {
    {
        game();
    };
    println("game over");
    debug_heap_count();
    0
}

fun debug_heap_count() {
    let heap_count = *mem:heap_count();
    println(&heap_count.to_string());
}